{
  "rules": {
    "users": {
      "$user": {
        ".read": "$user == auth.uid",
        ".write": "$user == auth.uid"
      }
    },
  "$other": {
      ".read": true,
      ".write": true
    }
  }
}